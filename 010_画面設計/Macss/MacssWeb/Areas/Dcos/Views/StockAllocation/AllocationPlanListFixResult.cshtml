
@{
    ViewBag.Title = MacssWeb.Resources.Dcos.AllocationPlanFixMainTitle;
    ViewBag.PageMainTitle = String.Format("{0}{1}", MacssWeb.Resources.Dcos.AllocationPlanFixMainTitle, "結果");
    ViewBag.ScreenId = "DCOS-D003-02";
}

<div class="alert alert-primary">
    <span>@ViewBag.ResultMessage</span>
</div>

<div class="row justify-content-between macss-form-button-group-row">
    <div class="col-12 text-right">
        <a role="button" class="btn btn-secondary" href="@Url.Action("Index")">@MacssWeb.Resources.ControlText.BackToList</a>
    </div>
</div>

<div class="container-fluid mt-4">
    <div class="form-inline">
        <div class="input-group mr-2 mb-1">
            <div class="input-group-prepend">
                <span class="input-group-text">確定済引当数</span>
            </div>
            <span class="form-control">@ViewBag.AllocCount</span>
        </div>
        @*<div class="input-group mr-2 mb-1">
            <div class="input-group-prepend">
                <span class="input-group-text">引当不能数数</span>
            </div>
            <span class="form-control">@ViewBag.ImpossibleAllocCount</span>
        </div>*@
    </div>
</div>

@Html.Partial("_List")
